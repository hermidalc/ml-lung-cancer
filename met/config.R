# config
dataset_names <- c(
    "lhc_nsclc"
)
data_types <- c(
    "met"
)
norm_methods <- c(
    "none"
)
feat_types <- c(
    "none"
)
prep_types <- c(
    "none",
    "cff",
    "mrg"
)
bc_methods <- c(
    "none",
    "ctr",
    "std",
    "rta",
    "rtg",
    "qnorm",
    "cbt",
    "fab",
    "sva",
    "stica0",
    "stica025",
    "stica05",
    "stica1",
    "svd"
)
filter_types <- c(
    "none"
)
common_pheno_names <- c(
    "Batch",
    "Class"
)
matfact_k <- 20
